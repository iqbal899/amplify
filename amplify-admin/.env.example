# Base URL of the amplify-backend Worker.
#
# Read on the Next server only — the browser never talks to the Worker directly,
# which is why the admin token can stay in an httpOnly cookie. Deliberately not
# NEXT_PUBLIC_, so it is not inlined into the client bundle.
AMPLIFY_API_URL=http://127.0.0.1:8787
